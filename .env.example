# Variáveis de ambiente — GP-Estacionamento
# Copie para .env e ajuste os valores locais.
# Não versionar o arquivo .env.

# Oracle
ORACLE_HOST=localhost
ORACLE_PORT=1521
ORACLE_SERVICE=FREEPDB1
ORACLE_USER=gp_app
ORACLE_PASSWORD=trocar_senha_oracle
ORACLE_URL=jdbc:oracle:thin:@//localhost:1521/FREEPDB1

# JWT (API Core — emissor)
JWT_SECRET=trocar_por_segredo_com_pelo_menos_32_caracteres

# BFF → API Core
CORE_BASE_URL=http://localhost:8080
